using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameTypes : MonoBehaviour
{
    public enum Bullets
    {
        normal = 0,
        shotgunShell = 1,
    }

    public enum Enemies
    {
        normal = 0,
        shooting = 1,
    }
}
